spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${ip}:3306/ie2024?useSSL=true
    username: root
    password: 123456

  web:
    resources:
      static-locations: file:/root/app/

logging:
  level:
    sql: info
    web: debug
    root: info

ip: mysql